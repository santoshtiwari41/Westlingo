import React from "react";

import CustomContainer from "@/components/common/custom-container";
import OnboardingForm from "@/modules/user/onboarding/components/form/onboarding-form";

const page = () => {
  return (
    <CustomContainer>
      <OnboardingForm />
    </CustomContainer>
  );
};

export default page;
